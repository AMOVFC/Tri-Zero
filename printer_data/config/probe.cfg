#####################################################################
# Probe
#####################################################################

[output_pin _probe_ready]
pin: PA10 #En #Enable pin
shutdown_value: 0
value:0

[probe]
##  Current probe: toolboard-mounted, signal on orbitoolO2S:PA9
##  Leviathan mainboard Z-PROBE pin is PF1 (kept for reference; not used).
pin: ^PA9 #out #Probe pin
# pin: ^PF1                          # Leviathan V1.1 Z-PROBE pin (mainboard) — switch to this if probe moves off toolboard
x_offset: 0                  # X-axis - sensor offset relative to nozzle
y_offset: 0                  # Y-axis - sensor offset relative to nozzle
z_offset:0                   # Z-axis - sensor offset relative to nozzle
speed: 5                     # Leveling speed
samples: 3                   # Number of samples
samples_result: median       # Value selection method (default: median)
sample_retract_dist: 2       # Retraction distance for leveling
samples_tolerance: 0.05      # Sample tolerance (note that excessively small values may increase sampling times)
samples_tolerance_retries: 10 # Retry count when exceeding tolerance
activate_gcode:
        G4 P500
        SET_PIN PIN=_probe_ready VALUE=1
deactivate_gcode:
        SET_PIN PIN=_probe_ready VALUE=0

#####################################################################
# Bed Leveling
#####################################################################

# [bed_tilt]
# # Order matters: FL, FR, BM (back middle)
# points:
#   15, 15        # Front Right
#   105, 15       # Front left
#   60, 105      # Back Middle
# speed: 50
# horizontal_move_z: 5
# retries: 3
# retry_tolerance: 0.01

[z_tilt]
z_positions:
  150, -5
  -30, -5
  60, 150
points:
  115, 5        # Front right
  5, 5       # Front left
  60, 115      # Back Middle
speed: 100
horizontal_move_z: 5
retries: 3
retry_tolerance: 0.05

[bed_mesh]
speed: 100
horizontal_move_z: 3
mesh_min: 15, 15
mesh_max: 105, 105
probe_count: 9, 9
algorithm: bicubic
bicubic_tension: 0.2

# [bed_screws]
# screw1: 60,5
# screw1_name: front screw
# screw2: 5,115
# screw2_name: back left
# screw3: 115,115
# screw3_name: back right

# [gcode_macro Bed_Level_Screws_Tune]
# gcode:
#     SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET=60
#     G28
#     TEMPERATURE_WAIT SENSOR=heater_bed MINIMUM=58 MAXIMUM=65
#     BED_SCREWS_ADJUST
#     TURN_OFF_HEATERS
